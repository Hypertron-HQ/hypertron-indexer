# Render sets NODE_ENV=production during the build. The Nest CLI, TypeScript,
# Jest typings, and other compiler inputs are devDependencies, so explicitly
# include them while building. Runtime still starts the compiled dist output.
include=dev
